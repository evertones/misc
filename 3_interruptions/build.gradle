/* This shows how to interrupt the execution of a task */

task eclipse << { println 'eclipse' }

eclipse.onlyIf {
    project.hasProperty('usingEclipse')
}

eclipse.doFirst {
    if(!usingEclipse) {
        throw new StopExecutionException()
    }
}
